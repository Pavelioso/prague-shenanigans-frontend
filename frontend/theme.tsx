import { DefaultTheme, Provider as PaperProvider } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    //primary: "#F77F00", // Custom primary color
    //accent: "#03dac4",  // Custom accent color
    //background: "#f6f6f6", // Custom background color
    //text: "#000000",   // Custom text color
    //chipBackground: "#FCBF49",
  },
};

export default theme;
